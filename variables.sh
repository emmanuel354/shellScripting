#!/usr/bin/bash

echo $BASH
#this prints the name of the shell which is in the BASH system variable

echo my bash version is $BASH_VERSION
echo my home directory is $HOME
echo my current working directory is $PWD

name=Elvis
echo the name is $name

